export default (code) => {
    // Allows any code to be run as a demo
    return {"success": true};
    /*
    // Stub code for checking the solution
    if (code.includes("catchFly(celebrate)")) {
        return true;
    }
    return false;
    */
};