// Delivers a package to the specified location
function deliverPackage(location) {
    // Returns a promise that resolves when the package is delivered
}

// Deliver all the packages to their destinations in sequence
async function deliverAll() {
    const locations = ["Bank", "School", "Hospital", "Government", "Library"];

    // Hint: Use a for loop to iterate over the delivery locations
    // and deliver a package to each one
    // Hint: Use await to wait for each package to be delivered

    const promises = [];
    for (let i = 0; i < locations.length; i++) {
        await deliverPackage(locations[i]);
    }
    
}

// Start the delivery process
deliverAll();
